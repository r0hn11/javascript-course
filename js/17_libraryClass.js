/*create a class library with following:
constructor with book list as argument
- getBookList
- issueBook(bookname, user)
- return(bookname)
*/
if(location.href.slice(location.href.lastIndexOf('/'),).includes('library.html')){}
else{location.href = '../projects/library.html'}